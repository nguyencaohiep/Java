
public class Pupil {
    /*

import java.util.HashMap;

public class SoKhacNhauTrongFile2 {
    public static void main(String[] args) throws IOException {
        HashMap<Integer, Integer> hm = new HashMap<Integer, Integer>();
        FileInputStream inp = new FileInputStream(new File("DATA.IN"));
        DataInputStream input = new DataInputStream(inp);

        for (int i = 0; i < 100000; i++) {
            int tmp = input.readInt();
            if (hm.get(tmp) == null) {
                hm.put(tmp, 1);
            } else {
                hm.replace(tmp, hm.get(tmp) + 1);
            }
        }

        ArrayList<Integer> keyArray = new ArrayList<>(hm.keySet());
        Collections.sort(keyArray);

        for (Integer i : keyArray) {
            System.out.println(i + " " + hm.get(i));
        }

        input.close();
    }
}

     */
}
